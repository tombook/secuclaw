# 技能执行历史后端持久化 — 设计

## 评估
当前 `sc-skill-execution-history.ts` 使用本地 `historyStore`。技能执行历史是低频查询数据，可在当前阶段保留本地存储，优先级低于其他业务闭环。

## 变更
1. 页面增加"导出历史"功能（JSON下载）
2. 保留本地存储 + 增加容量限制提示
3. 后端持久化作为后续迭代目标

## 约束
- 低优先级，不阻塞其他提案
