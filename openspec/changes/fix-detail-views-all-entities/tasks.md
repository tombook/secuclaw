# 全实体详情查看 — 任务

- [ ] 1. `sc-incidents-page.ts` 点击行调用 `getIncident(id)` 展开事件详情面板
- [ ] 2. `sc-vulnerabilities-page.ts` 点击行调用 `getVulnerability(id)` 展开漏洞详情面板
- [ ] 3. `sc-assets-page.ts` 点击行调用 `getAsset(id)` 展开资产详情面板（含关联漏洞）
- [ ] 4. `sc-threats-page.ts` 点击行调用 `getThreat(id)` 展开威胁详情面板
- [ ] 5. `sc-compliance-page.ts` 点击行调用 `getCompliance(id)` 展开合规详情面板
- [ ] 6. LSP 诊断 + Vite 构建验证
