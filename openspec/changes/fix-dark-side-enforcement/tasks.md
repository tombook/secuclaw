## 1. 后端 - 强制审批检查

- [x] 1.1 在黑暗面相关 route handler 中增加前置 `canExecute` 检查中间件
- [x] 1.2 确认所有黑暗面执行入口（渗透测试/红队/APT模拟）都经过检查
- [x] 1.3 未审批请求返回明确错误码和信息

## 2. 前端 - 审批拦截

- [x] 2.1 `sc-secops-center.ts` 黑暗面工具执行按钮点击时先检查 `approval.canExecute`
- [x] 2.2 未审批 → 弹出审批创建对话框（使用已有的 `sc-approval-dialog`）
- [x] 2.3 审批被拒 → 显示拒绝原因，禁用执行按钮
- [x] 2.4 `sc-domain-board.ts` 黑暗面任务执行增加相同拦截逻辑

## 3. 前端 - 路由守卫

- [x] 3.1 `auth-service.ts` 路由守卫增加 `dark-side.task.execute` 检查
- [x] 3.2 无权限用户隐藏黑暗面入口（菜单/按钮）

## 4. 验证

- [x] 4.1 验证未审批时执行被拒绝（前端+后端双重验证）
- [x] 4.2 验证审批通过后执行正常
- [x] 4.3 验证审批过期后被拒绝
- [x] 4.4 验证绕过前端直接调 API 也被后端拦截
